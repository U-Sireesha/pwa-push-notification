{
  "name": "pwa-full",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare-client": "cd my-app && npm install  && npm run build",
    "production": "npm install && npm run prepare-client && npx nodemon server.js --ignore my-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "web-push": "^3.5.0"
  }
}
